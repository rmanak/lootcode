# Dev/test tooling. Install: pip install -r requirements-dev.txt
-r requirements.txt
pytest>=8.0
pytest-cov>=5.0        # `make test-cov`; coverage config lives in pyproject.toml
httpx>=0.27            # for FastAPI TestClient
ruff>=0.13             # lint (+ format, opt-in via `make format`); config in pyproject.toml
mypy>=1.11             # type check; config in pyproject.toml
pre-commit>=3.7        # git hooks; config in .pre-commit-config.yaml
