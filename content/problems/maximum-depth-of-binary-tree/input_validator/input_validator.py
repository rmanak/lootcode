"""Machine-generated input-constraint validator for problem 'maximum-depth-of-binary-tree'.

Generated by scripts/generate_constraint_validators.py (model: local-model).
`validate_input(...)` returns True iff its arguments satisfy the input constraints
stated in the problem. Constraints deliberately NOT enforced:
  Enforced node count and value range constraints; skipped semantic guarantees about tree structure validity beyond basic traversal.
Do not edit by hand; re-run the generator instead.
"""

def validate_input(root):
    if root is None:
        return True
    
    if not isinstance(root, object):
        return False
    
    if not hasattr(root, 'val') or not hasattr(root, 'left') or not hasattr(root, 'right'):
        return False
    
    queue = [root]
    count = 0
    
    while queue:
        node = queue.pop(0)
        
        if node is None:
            continue
            
        if not isinstance(node, object):
            return False
            
        if not hasattr(node, 'val') or not hasattr(node, 'left') or not hasattr(node, 'right'):
            return False
            
        val = node.val
        if isinstance(val, bool) or not isinstance(val, int):
            return False
        if not (-100 <= val <= 100):
            return False
            
        count += 1
        if count > 10**4:
            return False
            
        queue.append(node.left)
        queue.append(node.right)
        
    return True
