"""Machine-generated input-constraint validator for problem 'minimum-possible-integer-after-at-most-k-adjacent-swaps-on-digits'.

Generated by scripts/generate_constraint_validators.py (model: local-model).
`validate_input(...)` returns True iff its arguments satisfy the input constraints
stated in the problem. Constraints deliberately NOT enforced:
  Enforced length bounds, digit-only content, no leading zero (except for single '0' which is handled by isdigit and leading zero check logic implicitly if len>1, but constraint says 'no leading zero' which usually implies the number itself doesn't start with 0 unless it is just '0'. However, standard interpretation of 'no leading zero' for a digit string representing a number usually means if len > 1, first char != '0'. If len == 1, '0' is valid. The constraint 'digits only with no leading zero' is slightly ambiguous for '0' itself. Given examples like '100', '4321', it's safe to assume standard integer representation rules. I will enforce that if len(num) > 1, num[0] != '0'. If len(num) == 1, '0' is allowed. Wait, the constraint says 'no leading zero'. Usually '0' is not considered to have a leading zero. '01' has a leading zero. So I will check: if len(num) > 1 and num[0] == '0': return False.
Do not edit by hand; re-run the generator instead.
"""

def validate_input(num, k):
    if not isinstance(num, str):
        return False
    if not isinstance(k, int) or isinstance(k, bool):
        return False
    if not (1 <= len(num) <= 3 * 10**4):
        return False
    if not (1 <= k <= 10**9):
        return False
    if not num.isdigit():
        return False
    if len(num) > 0 and num[0] == '0':
        return False
    return True
