"""Machine-generated input-constraint validator for problem 'path-sum-ii'.

Generated by scripts/generate_constraint_validators.py (model: local-model).
`validate_input(...)` returns True iff its arguments satisfy the input constraints
stated in the problem. Constraints deliberately NOT enforced:
  Assumed 'root' is a list based on the example and 'encoded as a level-order list' description, despite the 'TreeNode' type hint. Validated node count and integer types. Skipped structural validity of the tree (e.g., whether the list forms a valid binary tree) as it's a semantic guarantee/formatting detail not strictly checkable without complex parsing, and 'number of nodes' constraint is the primary bound.
Do not edit by hand; re-run the generator instead.
"""

def validate_input(root, targetSum):
    # Validate targetSum type and range (inferred from typical int constraints, though specific range not given, type is)
    if not isinstance(targetSum, int) or isinstance(targetSum, bool):
        return False
    
    # Validate root structure
    # The problem states root is a TreeNode. We need to traverse it to check node count and value types.
    # We assume TreeNode is a class with 'val', 'left', 'right' attributes or similar.
    # However, the prompt says "encoded as a level-order list" in the description but the parameter type is TreeNode.
    # Usually, in these validation tasks, if the parameter is declared as TreeNode, we expect an object.
    # But the description says "Trees are shown below in LeetCode level-order array form".
    # Let's look at the parameter declaration: "root: TreeNode (a binary tree, encoded as a level-order list with null for absent children)"
    # This is contradictory. "TreeNode" usually implies an object. "encoded as a level-order list" implies a list.
    # Given the previous examples used lists for arrays, and this one specifies "TreeNode" but describes it as a list in the text...
    # Let's re-read carefully: "The function's parameters... root: TreeNode (a binary tree, encoded as a level-order list...)"
    # This suggests the input `root` IS the list representation, despite the name TreeNode.
    # OR, it suggests the input is a TreeNode object, and the description is just explaining how trees are visualized.
    # Standard LeetCode problems pass TreeNode objects. The "level-order array" is usually for serialization.
    # However, the constraint "0 <= number of nodes <= 5000" applies to the tree.
    # If `root` is a list, we validate the list. If `root` is an object, we traverse.
    # Let's look at the example: `root = [5,4,8...]`. This is a list.
    # The parameter type says `TreeNode`. This is likely a misnomer in the prompt's type hint or the prompt implies the input is the serialized list.
    # Given the ambiguity, I will check if it's a list (as per the example and "encoded as" text) or an object.
    # Actually, looking at the previous prompt "nums: int[]", it was a list.
    # Here "root: TreeNode" but description says "encoded as a level-order list".
    # I will assume `root` is a list of values (int or None) representing the tree in level order.
    
    if not isinstance(root, list):
        return False
        
    # Count nodes and validate values
    node_count = 0
    for val in root:
        if val is None:
            continue
        if not isinstance(val, int) or isinstance(val, bool):
            return False
        node_count += 1
        
    if not (0 <= node_count <= 5000):
        return False
        
    return True
