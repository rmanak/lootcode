{
  "slug": "rate-limiter",
  "title": "Rate Limiter",
  "difficulty": "medium",
  "tags": [
    "design",
    "queue",
    "hash-table"
  ],
  "hints": [
    "Track each user's allowed request timestamps separately to count how many fall within the sliding window.",
    "Since timestamps are non-decreasing, store timestamps in a list or deque per user and remove those older than `timestamp - window`.",
    "Before allowing a new request, discard expired timestamps from the front of the user's history, then check if the remaining count is less than `limit`."
  ],
  "languages": [
    "python"
  ],
  "limits": {
    "timeLimitMs": 10000,
    "memoryLimitMb": 512
  },
  "kind": "class",
  "class": {
    "name": "RateLimiter",
    "constructor": {
      "params": [
        {
          "name": "limit",
          "type": "int"
        },
        {
          "name": "window",
          "type": "int"
        }
      ]
    },
    "methods": [
      {
        "name": "request",
        "params": [
          {
            "name": "userId",
            "type": "string"
          },
          {
            "name": "timestamp",
            "type": "int"
          }
        ],
        "returns": {
          "type": "bool"
        }
      }
    ]
  },
  "scoring": {
    "type": "weighted",
    "points": 100
  },
  "compare": "exact"
}
