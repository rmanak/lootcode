"""Machine-generated input-constraint validator for problem 'range-sum-mutable'.

Generated by scripts/generate_constraint_validators.py (model: local-model).
`validate_input(...)` returns True iff its arguments satisfy the input constraints
stated in the problem. Constraints deliberately NOT enforced:
  Enforced all checkable constraints: types, lengths, value ranges for indices. Did not enforce semantic guarantees about results.
Do not edit by hand; re-run the generator instead.
"""

def validate_input(nums, operations):
    # Check nums type and length
    if not isinstance(nums, list):
        return False
    if not (1 <= len(nums) <= 3 * 10**4):
        return False
    
    # Check nums elements are integers (not bools)
    for x in nums:
        if isinstance(x, bool) or not isinstance(x, int):
            return False
    
    # Check operations type and length
    if not isinstance(operations, list):
        return False
    if not (1 <= len(operations) <= 3 * 10**4):
        return False
    
    n = len(nums)
    
    # Check each operation
    for op in operations:
        if not isinstance(op, list):
            return False
        if len(op) != 3:
            return False
        
        cmd, arg1, arg2 = op
        
        # Command must be string "query" or "update"
        if not isinstance(cmd, str):
            return False
        
        # Arguments must be integers (not bools)
        if isinstance(arg1, bool) or not isinstance(arg1, int):
            return False
        if isinstance(arg2, bool) or not isinstance(arg2, int):
            return False
        
        if cmd == "query":
            l, r = arg1, arg2
            # 0 <= l <= r < len(nums)
            if not (0 <= l <= r < n):
                return False
        elif cmd == "update":
            i, val = arg1, arg2
            # 0 <= i < len(nums)
            if not (0 <= i < n):
                return False
        else:
            # Unknown command
            return False
            
    return True
