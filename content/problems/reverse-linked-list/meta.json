{
  "slug": "reverse-linked-list",
  "title": "Reverse Linked List",
  "difficulty": "easy",
  "tags": [
    "linked-list",
    "recursion"
  ],
  "hints": [
    "Walk the list keeping a `prev` pointer that trails one node behind the node you are visiting.",
    "At each node, save `head.next`, point `head.next` back to `prev`, then advance both `prev` and `head`.",
    "When `head` reaches `None`, `prev` is the new head of the fully reversed list."
  ],
  "languages": [
    "python"
  ],
  "limits": {
    "timeLimitMs": 10000,
    "memoryLimitMb": 512
  },
  "function": {
    "name": "reverseList",
    "params": [
      {
        "name": "head",
        "type": "ListNode"
      }
    ],
    "returns": {
      "type": "ListNode"
    }
  },
  "scoring": {
    "type": "weighted",
    "points": 100
  },
  "compare": "exact"
}
