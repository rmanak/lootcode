"""Machine-generated input-constraint validator for problem 'minimum-genetic-mutation'.

Generated by scripts/generate_constraint_validators.py (model: local-model).
`validate_input(...)` returns True iff its arguments satisfy the input constraints
stated in the problem. Constraints deliberately NOT enforced:
  Enforced equal length of startGene/endGene and the ACGT alphabet (start, end, and bank genes); did not impose a fixed gene length, which the constraints do not state. Skipped semantic guarantees about reachability.
Do not edit by hand; re-run the generator instead.
"""

def validate_input(startGene, endGene, bank):
    # Check types
    if not isinstance(startGene, str):
        return False
    if not isinstance(endGene, str):
        return False
    if not isinstance(bank, list):
        return False

    # Check bank length constraint
    if not (0 <= len(bank) <= 10):
        return False

    # Check that startGene and endGene have equal length
    if len(startGene) != len(endGene):
        return False

    # Check that startGene and endGene consist only of A, C, G, T
    allowed_chars = set('ACGT')
    if not all(c in allowed_chars for c in startGene):
        return False
    if not all(c in allowed_chars for c in endGene):
        return False

    # Check that all elements in bank are strings over A, C, G, T. The constraints
    # only state that startGene and endGene share a length over ACGT; they do not
    # fix a specific gene length (the test data uses genes shorter than 8), so we
    # do not impose one.
    for gene in bank:
        if not isinstance(gene, str):
            return False
        if not all(c in allowed_chars for c in gene):
            return False

    return True
