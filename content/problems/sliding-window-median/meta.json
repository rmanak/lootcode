{
  "slug": "sliding-window-median",
  "title": "Sliding Window Median",
  "difficulty": "hard",
  "tags": [
    "array",
    "sliding-window",
    "heap"
  ],
  "hints": [
    "A sliding window moves one step at a time, so the sorted order changes slightly; how can you update the median efficiently instead of resorting?",
    "Maintain two heaps: a max-heap for the lower half and a min-heap for the upper half of the current window.",
    "The median is the top of the larger heap; balance sizes by moving elements when sliding, handling removals lazily."
  ],
  "languages": [
    "python"
  ],
  "limits": {
    "timeLimitMs": 10000,
    "memoryLimitMb": 512
  },
  "function": {
    "name": "medianSlidingWindow",
    "params": [
      {
        "name": "nums",
        "type": "int[]"
      },
      {
        "name": "k",
        "type": "int"
      }
    ],
    "returns": {
      "type": "int[]"
    }
  },
  "scoring": {
    "type": "weighted",
    "points": 100
  },
  "compare": "exact"
}
