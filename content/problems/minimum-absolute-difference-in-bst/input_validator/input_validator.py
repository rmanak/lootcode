"""Machine-generated input-constraint validator for problem 'minimum-absolute-difference-in-bst'.

Generated by scripts/generate_constraint_validators.py (model: local-model).
`validate_input(...)` returns True iff its arguments satisfy the input constraints
stated in the problem. Constraints deliberately NOT enforced:
  Enforced node count [2, 10^4] by counting non-None elements in the level-order list, and value range [0, 10^5] for each node. Did not enforce BST property as it is a semantic guarantee/structural constraint not explicitly listed in the 'Constraints' section bounds, though implied by the problem type; however, standard input validation usually focuses on the explicit bounds provided.
Do not edit by hand; re-run the generator instead.
"""

def validate_input(root):
    if not isinstance(root, list):
        return False
    
    # The number of nodes in the tree is in the range [2, 10^4].
    # In LeetCode level-order representation, 'null' (None) represents missing children.
    # We must count only the actual nodes (non-None values) to check the node count constraint.
    node_count = 0
    for item in root:
        if item is None:
            continue
        if not isinstance(item, int) or isinstance(item, bool):
            return False
        if not (0 <= item <= 10**5):
            return False
        node_count += 1
    
    if not (2 <= node_count <= 10**4):
        return False
        
    return True
