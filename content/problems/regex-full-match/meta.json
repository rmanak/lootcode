{
  "slug": "regex-full-match",
  "title": "Regular Expression Full Match",
  "difficulty": "hard",
  "tags": [
    "string",
    "dynamic-programming",
    "recursion"
  ],
  "hints": [
    "Consider how the pattern can skip characters or repeat them, suggesting a recursive structure based on prefixes of both strings.",
    "Use dynamic programming where the state tracks whether a prefix of the pattern matches a prefix of the string.",
    "Define the base case for empty strings; for a star, decide whether to ignore the preceding element or consume more characters from the string."
  ],
  "languages": [
    "python"
  ],
  "limits": {
    "timeLimitMs": 10000,
    "memoryLimitMb": 512
  },
  "function": {
    "name": "isMatch",
    "params": [
      {
        "name": "s",
        "type": "string"
      },
      {
        "name": "p",
        "type": "string"
      }
    ],
    "returns": {
      "type": "bool"
    }
  },
  "scoring": {
    "type": "weighted",
    "points": 100
  },
  "compare": "exact"
}
