"""Machine-generated input-constraint validator for problem 'deepest-leaves-sum'.

Generated by scripts/generate_constraint_validators.py (model: local-model).
`validate_input(...)` returns True iff its arguments satisfy the input constraints
stated in the problem. Constraints deliberately NOT enforced:
  Assumed input is a level-order list based on 'encoded as a level-order list' description, despite the parameter name 'TreeNode'. Validated node count and value ranges.
Do not edit by hand; re-run the generator instead.
"""

def validate_input(root):
    # The problem states the input is a TreeNode object.
    # However, the example shows a list representation [1,2,3...].
    # The prompt says: "root: TreeNode (a binary tree, encoded as a level-order list with null for absent children)"
    # This is contradictory. Usually, if the parameter type is TreeNode, the input is an object.
    # If the parameter type is List, the input is a list.
    # The prompt explicitly says: "The function's parameters (name and declared type) are: - root: TreeNode"
    # But then it says "encoded as a level-order list". This usually implies the *serialization* format.
    # Let's look at the example: root = [1,2,3...]. This suggests the input might actually be a list.
    # BUT, the signature says `root: TreeNode`. And the description says "your function works with TreeNode objects".
    # Standard LeetCode problems pass TreeNode objects. The "level-order array" is just how it's displayed in the problem description.
    # However, the prompt says "encoded as a level-order list". This is a strong hint that the input IS a list.
    # Let's re-read carefully: "root: TreeNode (a binary tree, encoded as a level-order list with null for absent children)"
    # This phrasing is ambiguous. It could mean "The concept is a TreeNode, but the data passed is a list".
    # Or it could mean "The input is a TreeNode object".
    # Given the explicit type hint `TreeNode` in the parameter list, I should check for a TreeNode-like object.
    # But wait, if I check for a TreeNode object, I need to define what a TreeNode is.
    # The prompt doesn't provide a TreeNode class definition.
    # If the input is a list, I should check for a list.
    # Let's look at the constraints: "1 <= number of nodes <= 10^4". This applies to the tree structure.
    # If the input is a list, I can count the non-null elements.
    # If the input is a TreeNode object, I need to traverse it.
    
    # Let's assume the input is a list because of the phrase "encoded as a level-order list".
    # If it were a TreeNode object, it would likely just say "root: TreeNode".
    # The parenthetical explanation "encoded as..." suggests the raw data format.
    
    # Let's try to handle both or decide based on common patterns in these prompts.
    # Usually, if the type is `TreeNode`, the input is an object. If the type is `List[int]`, it's a list.
    # Here the type is `TreeNode`. But the description says "encoded as a level-order list".
    # This is a common confusion in problem descriptions. The *problem statement* shows the list.
    # The *function signature* takes a TreeNode.
    # In LeetCode, the driver code converts the list to a TreeNode before calling the function.
    # So the input to `validate_input` should be a TreeNode object.
    
    # However, I cannot import TreeNode. I must check for an object with `val`, `left`, `right` attributes.
    # Or, if the prompt implies the input IS the list, I should check for a list.
    
    # Let's look at the previous examples. They were straightforward.
    # If I assume it's a TreeNode object:
    # 1. Check if root is None (empty tree). But constraint says 1 <= nodes. So root cannot be None.
    # 2. Traverse the tree to count nodes and check values.
    
    # If I assume it's a list:
    # 1. Check if root is a list.
    # 2. Check length and values.
    
    # The prompt says: "root: TreeNode (a binary tree, encoded as a level-order list with null for absent children)"
    # This is extremely likely to mean the input IS the list, despite the type name "TreeNode" being used loosely to describe the data structure concept.
    # Why? Because "encoded as a level-order list" describes the *input format*.
    # If the input were a TreeNode object, it wouldn't be "encoded as a list". It would be a tree of objects.
    # So I will validate against a list.
    
    if not isinstance(root, list):
        return False
        
    # Constraint: 1 <= number of nodes <= 10^4
    # The list contains values and None. We need to count non-None values.
    nodes = [x for x in root if x is not None]
    if not (1 <= len(nodes) <= 10**4):
        return False
        
    # Constraint: 1 <= value <= 100
    for x in nodes:
        if not isinstance(x, int) or isinstance(x, bool):
            return False
        if not (1 <= x <= 100):
            return False
            
    # Check that all elements in the list are either int or None
    for x in root:
        if x is not None:
            if not isinstance(x, int) or isinstance(x, bool):
                return False
                
    return True
