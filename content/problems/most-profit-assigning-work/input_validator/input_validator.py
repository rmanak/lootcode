"""Machine-generated input-constraint validator for problem 'most-profit-assigning-work'.

Generated by scripts/generate_constraint_validators.py (model: local-model).
`validate_input(...)` returns True iff its arguments satisfy the input constraints
stated in the problem. Constraints deliberately NOT enforced:
  Enforced all checkable constraints: types, lengths, equality of difficulty/profit lengths, and value ranges. No semantic guarantees to skip.
Do not edit by hand; re-run the generator instead.
"""

def validate_input(difficulty, profit, worker):
    # Check types
    if not isinstance(difficulty, list):
        return False
    if not isinstance(profit, list):
        return False
    if not isinstance(worker, list):
        return False

    # Check lengths
    if not (1 <= len(difficulty) <= 10**4):
        return False
    if not (1 <= len(profit) <= 10**4):
        return False
    if not (1 <= len(worker) <= 10**4):
        return False
    
    # Check difficulty and profit have same length
    if len(difficulty) != len(profit):
        return False

    # Helper to check integer list constraints
    def check_int_list(lst, min_val, max_val):
        for x in lst:
            if isinstance(x, bool) or not isinstance(x, int):
                return False
            if not (min_val <= x <= max_val):
                return False
        return True

    # Check values in difficulty, profit, and worker are in [1, 10^5]
    if not check_int_list(difficulty, 1, 10**5):
        return False
    if not check_int_list(profit, 1, 10**5):
        return False
    if not check_int_list(worker, 1, 10**5):
        return False

    return True
