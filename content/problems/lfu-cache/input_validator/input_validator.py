"""Machine-generated input-constraint validator for problem 'lfu-cache'.

Generated by scripts/generate_constraint_validators.py (model: local-model).
`validate_input(...)` returns True iff its arguments satisfy the input constraints
stated in the problem. Constraints deliberately NOT enforced:
  All stated constraints are checkable and enforced.
Do not edit by hand; re-run the generator instead.
"""

def validate_input(capacity, operations):
    # Check capacity type and range
    if not isinstance(capacity, int) or isinstance(capacity, bool):
        return False
    if not (0 <= capacity <= 10**4):
        return False

    # Check operations type and length
    if not isinstance(operations, list):
        return False
    if not (1 <= len(operations) <= 10**4):
        return False

    # Check each operation
    for op in operations:
        if not isinstance(op, list):
            return False
        
        # Operations are either ["put", key, value] or ["get", key]
        if len(op) == 3:
            if op[0] != "put":
                return False
            key = op[1]
            value = op[2]
            # Check key and value types and ranges
            if not isinstance(key, int) or isinstance(key, bool):
                return False
            if not isinstance(value, int) or isinstance(value, bool):
                return False
            if not (0 <= key <= 10**9):
                return False
            if not (0 <= value <= 10**9):
                return False
        elif len(op) == 2:
            if op[0] != "get":
                return False
            key = op[1]
            # Check key type and range
            if not isinstance(key, int) or isinstance(key, bool):
                return False
            if not (0 <= key <= 10**9):
                return False
        else:
            return False

    return True
