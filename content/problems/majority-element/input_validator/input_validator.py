"""Machine-generated input-constraint validator for problem 'majority-element'.

Generated by scripts/generate_constraint_validators.py (model: local-model).
`validate_input(...)` returns True iff its arguments satisfy the input constraints
stated in the problem.

Manual edit 2026-07-08: added majority-element existence check.
The LLM generator originally skipped it as a "semantic guarantee", but without
it the test-strengthening engine generates inputs that violate the problem's
core guarantee (a majority element must exist).
"""
from collections import Counter


def validate_input(nums):
    if not isinstance(nums, list):
        return False
    n = len(nums)
    if not (1 <= n <= 5 * 10**4):
        return False
    for x in nums:
        if isinstance(x, bool) or not isinstance(x, int):
            return False
        if not (-10**9 <= x <= 10**9):
            return False
    # The problem guarantees a majority element (> floor(n/2) occurrences) exists.
    counts = Counter(nums)
    return max(counts.values()) > (n // 2)
