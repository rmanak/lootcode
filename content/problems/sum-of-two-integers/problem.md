Given two integers `a` and `b`, return **their sum** without using the `+` or `-`
operators.

## Constraints
- `-1000 <= a, b <= 1000`

## Examples
Input: `a = 1, b = 2`
Output: `3`

Input: `a = 2, b = 3`
Output: `5`
