def minTravelCost(target, capacity, position, price):
    """Return the minimum cost to drive `target` units, or -1 if impossible.

    The tank holds `capacity` units and starts full; driving burns 1 unit per
    unit of distance. Station i sits at position[i] and sells at price[i] per
    unit.
    """
    pass
