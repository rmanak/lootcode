"""Machine-generated input-constraint validator for problem 'linked-list-in-binary-tree'.

Generated by scripts/generate_constraint_validators.py (model: local-model).
`validate_input(...)` returns True iff its arguments satisfy the input constraints
stated in the problem. Constraints deliberately NOT enforced:
  Enforced length and value constraints for head and root. Counted non-None values in root to enforce the node count constraint. Did not enforce structural validity of the tree (e.g., whether the level-order list is well-formed) as it is not explicitly stated as a constraint, only the node count and value ranges.
Do not edit by hand; re-run the generator instead.
"""

def validate_input(head, root):
    # Validate head: must be a list of integers
    if not isinstance(head, list):
        return False
    if not (1 <= len(head) <= 100):
        return False
    for val in head:
        if isinstance(val, bool) or not isinstance(val, int):
            return False
        if not (1 <= val <= 100):
            return False

    # Validate root: must be a list representing the tree
    if not isinstance(root, list):
        return False
    
    # Tree must have between 1 and 2500 nodes.
    # In LeetCode level-order representation, the list length corresponds to the number of nodes
    # if we consider that trailing nulls are dropped. However, the constraint says "the tree has between 1 and 2500 nodes".
    # The input `root` is the level-order array. We need to count the actual nodes (non-None values) or just check the list length?
    # Usually, constraints on "number of nodes" refer to the count of actual TreeNode objects.
    # But since we are validating the raw input list, we should check if the list represents a valid tree structure within bounds.
    # The problem states "the tree has between 1 and 2500 nodes". This refers to the count of non-null entries in the conceptual tree.
    # However, the input `root` is a list. Let's assume the constraint applies to the number of elements in the `root` list that are not None,
    # or simply the length of the list if it's dense. Given the ambiguity, checking the length of the list `root` is the most direct checkable constraint
    # derived from "tree has ... nodes" if we assume the list length roughly correlates, but strictly speaking, we should count non-None values.
    # Let's count non-None values to be precise about "nodes".
    
    node_count = 0
    for val in root:
        if val is not None:
            if isinstance(val, bool) or not isinstance(val, int):
                return False
            if not (1 <= val <= 100):
                return False
            node_count += 1
        # None is allowed for missing children
        
    if not (1 <= node_count <= 2500):
        return False
        
    return True
