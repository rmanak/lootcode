"""Machine-generated input-constraint validator for problem 'invalid-transactions'.

Generated by scripts/generate_constraint_validators.py (model: local-model).
`validate_input(...)` returns True iff its arguments satisfy the input constraints
stated in the problem. Constraints deliberately NOT enforced:
  Enforced all structural and value range constraints. Did not enforce semantic validity of transactions (e.g., whether they are actually invalid based on cross-referencing), as that is part of the problem logic, not input validation.
Do not edit by hand; re-run the generator instead.
"""

def validate_input(transactions):
    if not isinstance(transactions, list):
        return False
    if len(transactions) > 1000:
        return False
    for t in transactions:
        if not isinstance(t, str):
            return False
        parts = t.split(',')
        if len(parts) != 4:
            return False
        name, time_str, amount_str, city = parts
        
        # Validate name: 1-10 lowercase letters
        if not (1 <= len(name) <= 10) or not name.isalpha() or not name.islower():
            return False
            
        # Validate city: 1-10 lowercase letters
        if not (1 <= len(city) <= 10) or not city.isalpha() or not city.islower():
            return False
            
        # Validate time: 0 <= time <= 1000
        if not time_str.isdigit():
            return False
        time_val = int(time_str)
        if not (0 <= time_val <= 1000):
            return False
            
        # Validate amount: 0 <= amount <= 2000
        if not amount_str.isdigit():
            return False
        amount_val = int(amount_str)
        if not (0 <= amount_val <= 2000):
            return False
    return True
