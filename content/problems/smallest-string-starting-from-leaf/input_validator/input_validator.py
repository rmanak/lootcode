"""Machine-generated input-constraint validator for problem 'smallest-string-starting-from-leaf'.

Generated by scripts/generate_constraint_validators.py (model: local-model).
`validate_input(...)` returns True iff its arguments satisfy the input constraints
stated in the problem. Constraints deliberately NOT enforced:
  Treated 'root' as a level-order list based on the parenthetical description and examples, validating node count (1-8500) and values (0-25).
Do not edit by hand; re-run the generator instead.
"""

def validate_input(root):
    # The problem states the input is a TreeNode object.
    # However, the examples and description mention "level-order array form".
    # The parameter declaration says "root: TreeNode".
    # Usually, in these validation tasks, if the parameter is declared as a specific object type (TreeNode),
    # we validate that object structure. If it were declared as a list, we'd validate the list.
    # Given the ambiguity of "encoded as a level-order list" in the prompt's parameter description
    # vs "TreeNode objects" in the statement, and the standard LeetCode format where `root` is a TreeNode,
    # we must decide which representation to validate.
    
    # Re-reading the prompt's parameter description:
    # "root: TreeNode (a binary tree, encoded as a level-order list with null for absent children)"
    # This is contradictory. A TreeNode is an object. A level-order list is a list.
    # However, looking at previous examples in this dataset style, usually the "parameter type" 
    # indicates the Python type passed to the function. If it says "TreeNode", it's an object.
    # But the parenthetical says "encoded as a level-order list". This often implies the input 
    # IS the list representation, despite the name "root" or the label "TreeNode".
    # Let's look at the constraints: "1 <= number of nodes <= 8500". This applies to the tree structure.
    # "each value is 0..25".
    
    # If I assume it's a TreeNode object:
    # I need to traverse it. I need to check node values are ints 0-25.
    # I need to check the number of nodes is 1-8500.
    
    # If I assume it's a list (level-order):
    # I need to check it's a list.
    # I need to check elements are ints 0-25 or None.
    # I need to check the number of non-None elements is 1-8500.
    
    # The prompt says: "The function's parameters (name and declared type) are: root: TreeNode".
    # And then in parentheses: "(a binary tree, encoded as a level-order list with null for absent children)".
    # This specific phrasing "encoded as..." usually describes the *data structure* passed in 
    # when the platform serializes the input. In many LeetCode-style validators for this platform,
    # if the type is listed as a custom class like TreeNode, but the description says it's encoded as a list,
    # it often means the input IS the list. 
    # HOWEVER, standard Python LeetCode solutions receive TreeNode objects.
    # Let's look at the "Rules": "Enforce only constraints that are checkable from the raw input alone".
    # If the input is a TreeNode object, I can check its structure.
    # If the input is a list, I can check its structure.
    
    # Let's look at the example: `root = [0,1,2,3,4,3,4]`. This is a list literal.
    # This strongly suggests the input `root` is a list.
    # The label "TreeNode" in the parameter list might be a misnomer or referring to the logical type.
    # The parenthetical "encoded as a level-order list" is the key.
    
    # Therefore, I will validate `root` as a list.
    
    if not isinstance(root, list):
        return False
        
    # Constraint: 1 <= number of nodes <= 8500
    # We count non-None values as nodes.
    node_count = 0
    for val in root:
        if val is None:
            continue
        if not isinstance(val, int) or isinstance(val, bool):
            return False
        if not (0 <= val <= 25):
            return False
        node_count += 1
        
    if not (1 <= node_count <= 8500):
        return False
        
    return True
