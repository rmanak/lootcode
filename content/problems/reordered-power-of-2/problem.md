**Return `true` if the digits of `N` can be rearranged (no leading zero) to form a
power of `2`.**

**Examples**
```
N = 1   ->  true
N = 10  ->  false
N = 16  ->  true
N = 46  ->  true
```

**Constraints:** `1 <= N <= 10^9`.
