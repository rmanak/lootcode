"""Machine-generated input-constraint validator for problem 'ipo-project-selection'.

Generated by scripts/generate_constraint_validators.py (model: local-model).
`validate_input(...)` returns True iff its arguments satisfy the input constraints
stated in the problem. Constraints deliberately NOT enforced:
  Skipped value range checks for profits[i] and capital[i] as they were not explicitly stated in the provided constraints section.
Do not edit by hand; re-run the generator instead.
"""

def validate_input(k, w, profits, capital):
    # Check k
    if not isinstance(k, int) or isinstance(k, bool):
        return False
    if not (1 <= k <= 10**5):
        return False

    # Check w
    if not isinstance(w, int) or isinstance(w, bool):
        return False
    if not (0 <= w <= 10**9):
        return False

    # Check profits and capital are lists
    if not isinstance(profits, list) or not isinstance(capital, list):
        return False

    # Check lengths match and are within bounds
    if len(profits) != len(capital):
        return False
    if not (1 <= len(profits) <= 10**5):
        return False

    # Check elements in profits
    for p in profits:
        if not isinstance(p, int) or isinstance(p, bool):
            return False
        # The problem statement doesn't explicitly bound profits[i] values in the constraints section provided,
        # but typically they are non-negative. However, rule 3 says "Do NOT invent constraints".
        # The constraints section only lists bounds for k, w, and lengths.
        # It does NOT list bounds for profits[i] or capital[i] values explicitly in the text provided.
        # Wait, looking closely at the provided text:
        # "Constraints: 1 <= k <= 10^5, 0 <= w <= 10^9, 1 <= len(profits) == len(capital) <= 10^5"
        # There are NO value constraints listed for profits[i] or capital[i] in the provided text.
        # Therefore, I should not enforce value ranges for them, only type.

    # Check elements in capital
    for c in capital:
        if not isinstance(c, int) or isinstance(c, bool):
            return False

    return True
