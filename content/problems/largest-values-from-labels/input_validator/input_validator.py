"""Machine-generated input-constraint validator for problem 'largest-values-from-labels'.

Generated by scripts/generate_constraint_validators.py (model: local-model).
`validate_input(...)` returns True iff its arguments satisfy the input constraints
stated in the problem. Constraints deliberately NOT enforced:
  All stated constraints are checkable from the raw input and were enforced.
Do not edit by hand; re-run the generator instead.
"""

def validate_input(values, labels, numWanted, useLimit):
    # Check types for lists
    if not isinstance(values, list) or not isinstance(labels, list):
        return False
    
    # Check types for integers (excluding bools)
    if not isinstance(numWanted, int) or isinstance(numWanted, bool):
        return False
    if not isinstance(useLimit, int) or isinstance(useLimit, bool):
        return False
    
    # Check length constraints: 1 <= len(values) == len(labels) <= 2*10^4
    if len(values) != len(labels):
        return False
    if not (1 <= len(values) <= 2 * 10**4):
        return False
    
    # Check element constraints for values and labels
    # 0 <= values[i], labels[i] <= 2*10^4
    for v in values:
        if not isinstance(v, int) or isinstance(v, bool):
            return False
        if not (0 <= v <= 2 * 10**4):
            return False
            
    for l in labels:
        if not isinstance(l, int) or isinstance(l, bool):
            return False
        if not (0 <= l <= 2 * 10**4):
            return False
            
    # Check numWanted and useLimit constraints
    # 1 <= numWanted, useLimit <= len(values)
    if not (1 <= numWanted <= len(values)):
        return False
    if not (1 <= useLimit <= len(values)):
        return False
        
    return True
