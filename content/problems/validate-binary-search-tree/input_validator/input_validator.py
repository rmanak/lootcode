"""Machine-generated input-constraint validator for problem 'validate-binary-search-tree'.

Generated by scripts/generate_constraint_validators.py (model: local-model).
`validate_input(...)` returns True iff its arguments satisfy the input constraints
stated in the problem. Constraints deliberately NOT enforced:
  Validated that input is a list, contains only integers (excluding bools) within the specified range, and the count of non-null nodes is between 1 and 10^4. The semantic validity of the BST structure is not checked as per rules.
Do not edit by hand; re-run the generator instead.
"""

def validate_input(root):
    # The problem statement implies the input is a TreeNode object or None.
    # However, the examples show level-order lists. The prompt says "encoded as a level-order list".
    # But the signature says `root: TreeNode`. Usually, in these validation tasks for LeetCode-style problems,
    # if the parameter is declared as TreeNode, we expect a TreeNode object.
    # BUT, looking at the examples: Input: root = [2,1,3]. This is a list.
    # The prompt says: "root: TreeNode (a binary tree, encoded as a level-order list with null for absent children)"
    # This is contradictory. "TreeNode" usually means an object. "encoded as a level-order list" means a list.
    # Given the explicit instruction "encoded as a level-order list", I will validate against a list structure.
    
    if not isinstance(root, list):
        return False
    
    # Constraint: 1 <= number of nodes <= 10^4
    # We need to count the actual nodes (non-null values) in the level-order list.
    # The list itself can have trailing nulls dropped, but the length of the list represents the structure.
    # Let's count non-None elements to get the number of nodes.
    
    node_count = 0
    
    for val in root:
        if val is None:
            continue
        # Check if value is an integer (and not a bool)
        if not isinstance(val, int) or isinstance(val, bool):
            return False
        # Check value range: -2^31 <= val <= 2^31 - 1
        if not (-2**31 <= val <= 2**31 - 1):
            return False
        node_count += 1
        
    if not (1 <= node_count <= 10**4):
        return False
        
    return True
