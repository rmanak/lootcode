"""Machine-generated input-constraint validator for problem 'kth-smallest-element-in-a-bst'.

Generated by scripts/generate_constraint_validators.py (model: local-model).
`validate_input(...)` returns True iff its arguments satisfy the input constraints
stated in the problem. Constraints deliberately NOT enforced:
  Enforced all checkable constraints: k is int, root is a valid tree with n nodes (1 <= n <= 10^4), 1 <= k <= n, and node values are ints in [-10^4, 10^4]. Did not enforce BST property as it is a semantic guarantee about the tree structure, not a simple input bound.
Do not edit by hand; re-run the generator instead.
"""

def validate_input(root, k):
    # Validate k
    if not isinstance(k, int) or isinstance(k, bool):
        return False
    
    # Validate root structure and collect nodes
    if root is None:
        return False
    
    # We need to traverse the tree to count nodes and check values.
    # The input 'root' is a TreeNode object.
    # We must ensure it's a valid tree structure (no cycles, proper node types).
    # Since we can't import TreeNode, we assume the object has 'val', 'left', 'right' attributes.
    # However, the problem statement says "TreeNode objects (value, left, right)".
    # Let's assume standard LeetCode TreeNode structure: val, left, right.
    
    nodes = []
    stack = [root]
    visited = set()
    
    while stack:
        node = stack.pop()
        if node is None:
            continue
        
        # Check if node is a valid object (not a primitive)
        if not hasattr(node, 'val'):
            return False
            
        # Check for cycles using id
        node_id = id(node)
        if node_id in visited:
            return False
        visited.add(node_id)
        
        nodes.append(node)
        
        # Check children
        if hasattr(node, 'left'):
            stack.append(node.left)
        if hasattr(node, 'right'):
            stack.append(node.right)
            
    n = len(nodes)
    
    # Constraint: 1 <= n <= 10^4
    if not (1 <= n <= 10**4):
        return False
        
    # Constraint: 1 <= k <= n
    if not (1 <= k <= n):
        return False
        
    # Constraint: -10^4 <= node.value <= 10^4
    for node in nodes:
        val = node.val
        if not isinstance(val, int) or isinstance(val, bool):
            return False
        if not (-10**4 <= val <= 10**4):
            return False
            
    return True
