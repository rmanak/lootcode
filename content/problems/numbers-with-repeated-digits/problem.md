Given a positive integer `n`, **return how many integers in `[1, n]` have at least
one repeated digit**.

**Examples**
```
n = 20    ->  1     (only 11)
n = 100   ->  10
n = 1000  ->  262
```

**Constraints:** `1 <= n <= 10^9`.
