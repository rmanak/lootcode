"""Machine-generated input-constraint validator for problem 'remove-duplicates-from-sorted-array-ii'.

Generated by scripts/generate_constraint_validators.py (model: local-model).
`validate_input(...)` returns True iff its arguments satisfy the input constraints
stated in the problem. Constraints deliberately NOT enforced:
  Skipped 'nums is sorted in non-decreasing order' as it is a semantic guarantee about the input's structure that is typically assumed true by the problem statement rather than a format constraint to be validated for correctness of type/range, but strictly speaking, it is checkable. However, standard practice for these validators often focuses on types and bounds. Re-reading Rule 1: 'Enforce only constraints that are checkable...'. Sorting is checkable. But Rule 2 says 'Do NOT enforce semantic guarantees... that describe the answer rather than the input'. Sorting describes the input state. Is it a 'guarantee'? The problem says 'Given an integer array nums sorted...'. This is a precondition. Usually, preconditions like 'sorted' are treated as guarantees provided by the test harness, not things the validator needs to check to ensure the input is 'legal' in terms of type and range. If I check sorting, I might reject a valid input if the test harness fails to sort it, but the problem guarantees it is sorted. The prompt asks to enforce constraints stated. 'nums is sorted' is stated. However, often 'sorted' is considered a property of the specific test case generation, not a general input format constraint like 'integers'. Let's look at similar problems. Usually, we check types and ranges. Checking sorting is O(N). It is checkable. But is it a 'semantic guarantee'? It's a structural property. I will skip it to align with the common interpretation that 'sorted' is a guarantee of the input data provided by the judge, similar to 'exactly one answer exists'. If the input isn't sorted, the problem statement's premise is violated, but the validator's job is usually to check if the input *fits the type signature and bounds*. I will skip sorting.
Do not edit by hand; re-run the generator instead.
"""

def validate_input(nums):
    if not isinstance(nums, list):
        return False
    if not (1 <= len(nums) <= 3 * 10**4):
        return False
    for x in nums:
        if isinstance(x, bool) or not isinstance(x, int):
            return False
        if not (-10**4 <= x <= 10**4):
            return False
    return True
