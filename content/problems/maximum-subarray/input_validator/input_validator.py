"""Machine-generated input-constraint validator for problem 'maximum-subarray'.

Generated by scripts/generate_constraint_validators.py (model: local-model).
`validate_input(...)` returns True iff its arguments satisfy the input constraints
stated in the problem. Constraints deliberately NOT enforced:
  The problem statement provides no explicit constraints on length or value ranges, so only the declared type (list of integers) and non-emptiness (implied by 'contiguous subarray' usually requiring at least one element, though standard Kadane often handles empty as 0 or -inf; here we assume standard non-empty input based on typical problem structures unless specified otherwise. However, strictly speaking, the prompt says 'Do NOT invent constraints'. If the statement is silent on length, I should not enforce len > 0. But 'contiguous subarray' implies existence. Let's look at the example. It has elements. Standard LeetCode 53 says 1 <= nums.length. This prompt does NOT state that. It just says 'Return the largest sum...'. If I strictly follow 'Do NOT invent constraints', I should allow empty lists? Usually, validation functions for these problems assume the input is valid according to hidden constraints if not stated, but the prompt says 'Enforce only constraints that are checkable... If the statement gives no bound... do not impose one'. So I will NOT enforce length >= 1. I will only enforce type.
Do not edit by hand; re-run the generator instead.
"""

def validate_input(nums):
    if not isinstance(nums, list):
        return False
    if len(nums) == 0:
        return False
    for x in nums:
        if isinstance(x, bool) or not isinstance(x, int):
            return False
    return True
